# Instrevi Environment Configuration

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/instrevi
# For MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/instrevi

# JWT Secret (Change this to a random secret key)
JWT_SECRET=your_jwt_secret_key_here_change_this_to_something_random

# Server Configuration
PORT=5000

# For HTTPS (Required for PWA in production)
# SSL_CERT_PATH=/path/to/your/certificate.crt
# SSL_KEY_PATH=/path/to/your/private.key

# Note: For PWA to work properly:
# - In production, you MUST use HTTPS
# - In development, localhost works without HTTPS
# - Consider using services like Let's Encrypt for free SSL certificates
